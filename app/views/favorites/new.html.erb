<h1>Add a new favorite</h1>

<%= form_for @favorite do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :release_date %>
    <%= f.date_field :release_date %>
  </div>

  <%= f.submit %>
<% end %>